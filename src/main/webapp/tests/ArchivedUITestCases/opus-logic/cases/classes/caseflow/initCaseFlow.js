import * as testHelpers from '../../../../test-utility/helpers';
import {constants} from '../../../../test-utility/testing-constants';

let defaultConstants = {
  name: 'cuff, dana',
  getNameSearchResults: '/restServices/rest/activecase/getNamesearchResultset/'
};

async function initCaseFlowCache(constants = defaultConstants)  {

  let {karmaAliasBaseUrl} = constants;


};
